org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Kotlin's incremental compiler computes relative paths between source files for its caches.
# This project (E:) and the pub cache (C:) live on different drive roots, which makes that
# relativization throw and corrupts the incremental cache. Disable incremental compilation to avoid it.
kotlin.incremental=false
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
